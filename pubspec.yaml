name: jump_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_drawing: ^1.0.1
  syncfusion_flutter_core: ^20.4.42
  syncfusion_flutter_sliders: ^20.4.42
  syncfusion_flutter_gauges: ^20.4.43
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/key_frames_axel5.png
    - assets/images/key_frames_lutz3.png
    - assets/images/key_frames_back_somersault4.png
